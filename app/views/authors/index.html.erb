<h1>
  Authors
</h1>

<% @authors.each do |author| %>
  <h2><%= author.name %></h2>
  <ul>
  <% author.books.each do |book| %>
    <li><%= link_to book.title, book_path(book) %></li>
  <% end %>
  </ul>
<% end %>